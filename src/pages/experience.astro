---
import Layout from "../layouts/Layout.astro";
import { experienceData } from "../data/experience";
---

<Layout
    title="Expériences - Léonel VODOUNOU"
    description="Mon parcours professionnel et mes expériences en IA et mathématiques appliquées"
>
    <main class="pt-20">
        <!-- Hero Section -->
        <section class="container py-12">
            <div class="text-center mb-12">
                <h1 class="heading-1 mb-4">Expériences Professionnelles</h1>
                <p
                    class="body-large max-w-2xl mx-auto text-gray-600 dark:text-gray-400"
                >
                    Mon parcours professionnel au croisement des mathématiques,
                    de l'IA et de l'innovation technologique
                </p>
            </div>
        </section>

        <!-- Timeline -->
        <section class="container pb-16">
            <div class="max-w-4xl mx-auto">
                {
                    experienceData.map((exp, index) => (
                        <div class="relative border-l-2 border-blue-200 dark:border-blue-800 ml-6 pb-12">
                            <div class="absolute -left-3 bg-blue-500 w-6 h-6 rounded-full border-4 border-white dark:border-gray-900" />
                            <div class="ml-8">
                                <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between mb-2">
                                    <h3 class="heading-3 text-blue-600 dark:text-blue-400">
                                        {exp.role}
                                    </h3>
                                    <span class="text-sm text-gray-500 dark:text-gray-400 font-medium">
                                        {exp.startDate} - {exp.endDate}
                                    </span>
                                </div>
                                <p class="text-lg font-medium text-gray-700 dark:text-gray-300 mb-3">
                                    {exp.company}
                                </p>
                                <p class="body-text mb-4">{exp.description}</p>
                                <div class="flex flex-wrap gap-2">
                                    {exp.technologies.map((tech) => (
                                        <span class="px-3 py-1 bg-blue-100 dark:bg-blue-900/30 text-blue-600 dark:text-blue-400 rounded-full text-sm">
                                            {tech}
                                        </span>
                                    ))}
                                </div>
                            </div>
                        </div>
                    ))
                }

                <!-- Final dot -->
                <div
                    class="relative border-l-2 border-blue-200 dark:border-blue-800 ml-6"
                >
                    <div
                        class="absolute -left-3 bg-gray-300 dark:bg-gray-600 w-6 h-6 rounded-full border-4 border-white dark:border-gray-900"
                    >
                    </div>
                </div>
            </div>
        </section>

        <!-- Skills & Achievements -->
        <section class="bg-gray-50 dark:bg-gray-900/50 py-16">
            <div class="container">
                <div class="text-center mb-12">
                    <h2 class="heading-2 mb-4">Réalisations Clés</h2>
                    <p
                        class="body-large max-w-2xl mx-auto text-gray-600 dark:text-gray-400"
                    >
                        Quelques achievements marquants de mon parcours
                        professionnel
                    </p>
                </div>

                <div
                    class="grid gap-8 md:grid-cols-2 lg:grid-cols-3 max-w-6xl mx-auto"
                >
                    <div
                        class="bg-white dark:bg-gray-800 p-6 rounded-xl shadow-sm border dark:border-gray-700"
                    >
                        <div class="text-3xl mb-4">📊</div>
                        <h3 class="heading-3 mb-2">Modèles en Production</h3>
                        <p class="body-text">
                            Déploiement de 5+ modèles ML en production avec une
                            amélioration moyenne de 25% des performances
                        </p>
                    </div>

                    <div
                        class="bg-white dark:bg-gray-800 p-6 rounded-xl shadow-sm border dark:border-gray-700"
                    >
                        <div class="text-3xl mb-4">📝</div>
                        <h3 class="heading-3 mb-2">Publications</h3>
                        <p class="body-text">
                            Co-auteur de 3 publications sur l'optimisation
                            stochastique et l'apprentissage automatique
                        </p>
                    </div>

                    <div
                        class="bg-white dark:bg-gray-800 p-6 rounded-xl shadow-sm border dark:border-gray-700"
                    >
                        <div class="text-3xl mb-4">🏆</div>
                        <h3 class="heading-3 mb-2">Prix & Reconnaissances</h3>
                        <p class="body-text">
                            Lauréat du concours de modélisation mathématique et
                            prix d'excellence académique
                        </p>
                    </div>
                </div>
            </div>
        </section>
    </main>
</Layout>
